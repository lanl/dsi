mcp_servers:
  # Connect the DSI tools via the stdio transport
  dsi:
    command: /Users/pascalgrosset/projects/dsi/tools/ai_search/mcp/dsi_mcp_server.py
    args:
      - --transport=streamable-http
# # Or Connect the DSI tools via the streamable-http transport
# Uncomment this and comment out the stdio transport config above
# dsi:
#   url: http://localhost:8080/mcp # Replace with the correct hostname + port
